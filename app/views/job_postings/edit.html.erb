<h1>Editing Job Posting</h1>

<%= render 'form', job_posting: @job_posting %>

<% if @admin %>

<%= link_to 'Show', admin_job_posting_path(@job_posting) %> |
<%= link_to 'Back', admin_job_postings_path %>

<% else %>

<%= link_to 'Show', user_job_posting_path(@job_posting) %> |
<%= link_to 'Back', user_job_postings_path %>

<% end %>